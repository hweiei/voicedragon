# 依赖与构建产物
node_modules/
dist/
dev-dist/
*.local

# 工具与缓存
.cache/
.vite/
coverage/
*.tsbuildinfo

# 浏览器自动化残留（历史上误提交过）
.browser-profile/
.browser-profile-voice/
.wxmg-port/
.workbuddy-ai/
.playwright/

# 系统文件
.DS_Store
Thumbs.db

# 环境变量与密钥（绝不入库）
.env
.env.*
*.pem
*.key

# ASR 模型（体积大，运行时从 CDN/镜像下载并缓存到 IndexedDB）
models/
public/models/
*.onnx
*.data
test-results/

# Local release packages (build output, not source)
releases/*.zip
releases/*.sha256
