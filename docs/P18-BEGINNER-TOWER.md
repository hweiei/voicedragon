# P18 新手教学塔 · 第一批

## 交付
- 默认入口：港风夜色六层教学塔；`?mode=classic` 保留原版冒险。
- 独立 beginner 模块与 `voice-dragon-beginner-v1` 存档，不改变原版战斗内核与存档。
- 6 句内容：问候、请求、点餐、问价、感谢、综合点单；普通话释义、粤拼、提示、场景题。
- 粤语系统朗读、慢速示范、本机 MediaRecorder 录音、回放、阅读降级。
- 设备无粤语音色时不播放替代声音；补充 yue 标签支持。共享 TTS speak 阻止非粤语兜底。
- 奖励三选一（慢声、提示、纪念章），固定教学顺序，种子仅影响部分场景名。尚不是新的完整分支 Roguelike；原版提供复杂冒险。
- 录音不上传，不做 ASR 或发音评测。录音完成仅表示生成音频，不证明有效开口；阅读不计口语完成。

## 验证
- Vitest：71 文件、593 测试通过（包含新增 5 条）。
- TypeScript + Vite 生产构建通过。
- Chromium 自动化：阅读六层通关、错答阻挡、阅读不计口语、刷新恢复、原版入口、390px 无横向溢出、无运行时异常。
- 模拟麦克风：录制/停止/回放入口、录音次数、存档不含音频引用通过；不等于真机录音或音质验收。
- 桌面与手机截图位于 outputs/beginner/。
- 未执行全部旧版 E2E/发布矩阵：旧 E2E 默认访问根路径，需改为 ?mode=classic 后再回归；未进行实际粤语音色、Safari 真机验证。

## 后续
1. 母语审校和可靠的固定示范音频；当前示范依赖设备音色。
2. 拆分旧内容中的释义和可接受答案；本批未批量修改旧版词库或评分。
3. 把教学进度接入长期 SRS；当前新手塔与原版学习档案独立。
4. 在学习前置约束下加入真实分岔路线、情境变体和学习驱动奖励。

本批仅在工作区修改，未推送 GitHub。

## 推送前复核（2026-09-25）
- SSH 仓库部署密钥认证成功；基于远端 main a04a9e9 创建独立评审分支。
- 本次重新运行 TypeScript/Vite 构建、9 条新手/TTS 针对测试、全仓 Biome，均通过。
- JS gzip 140.7 / 350 KB，发布产物契约 44 项通过。
- 全量 593 条测试为本批开发阶段运行结果；本次推送前未再次全量运行。
- 旧版 E2E 入口适配仍待完成，当前分支不宣称完整发布 CI 已通过，不自动合并或部署。

## P18-F2 · 发布回归与入口兼容（2026-09-25）

本节取代上一批“旧 E2E 待适配”的状态描述。

### 修复
- 旧版测试明确使用 `?mode=classic`；加载/刷新后等待真实引擎就绪，避免动态入口加载的竞态。
- 原有 `?duel=...#c=...` 和仅 `#c=...` 的分享链接仍进入原版；清除挑战片段后刷新不会落回新手页。
- 将新手存档校验抽成纯函数：顺序/范围/奖励白名单验证，过滤未知和重复口语记录，损坏存档安全重置，不动原版存档。
- 奖励尚未领取时刷新恢复奖励页；粤拼灯牌提示在刷新后保留。
- 重录清除当前完成标志，录制期间不能使用上次录音状态过关。
- 改善路线数字对比度与教学卡标题语义，不降低 Lighthouse 阈值。

### 本轮实测
- `npm run ci`：全仓 Biome、TypeScript、72 文件 **597 测试**、生产构建、性能预算、44 项发布契约通过。
- Chromium 业务回归：**62/62**（原 55 + 新手 7），`--workers=1`；首次高并发运行触及资源限制且暴露入口加载竞态，修复并完整串行复测，不隐瞒首次失败。
- 浏览器发布矩阵：**26 通过 / 2 明确跳过**。包括 Chromium 桌面/移动、Firefox、WebKit 移动模拟；新手和原版各一项 WebKit 离线模拟因已知限制跳过，仍待真机。
- 原版视觉回归：**16/16**，未更新/放宽原有截图基线。
- Lighthouse 最新：首页移动三次中位数 **100/100/96/100**，桌面 **100/100/100/100**（性能/无障碍/最佳实践/SEO）；移动 LCP 1319ms、TBT 38ms、CLS .002；桌面 LCP 342ms、TBT 0ms、CLS .001。首轮桌面 A11y=93 不达标，修正后重跑通过。
- 全浏览器测试均为自动化环境；模拟麦克风不能代替真实粤语、真实麦克风、Safari 真机验收。

### 未改变的边界
- 未引入真实发音评分、固定示范音频或已审校词库；设备无粤语音色时仍明确降级。
- 本轮不合并 main、不自动部署；部署密钥只能用于 Git SSH，未配置 GitHub API 凭据来创建 PR。
- 最终补验：GitHub Pages `/voicedragon/` 子路径构建与 44 项发布契约通过；恢复默认构建后跨浏览器 26/2 与视觉 16/16 再次通过；JS gzip 141.0/350 KB。
- Playwright 默认 workers=1 以控制浏览器内存占用，可用 CLI --workers 在资源充足机器覆盖，不修改超时或断言阈值。
